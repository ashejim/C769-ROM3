# uv / Python virtual environment — recreated locally via `uv sync`, never committed
.venv/
__pycache__/
*.pyc

# editor / build cruft
.history/
.ipynb_checkpoints/

# GitLab Pages output — generated by CI from _build/html, never committed
public/
